{
  "name": "cli-tools",
  "inputs": ["Command line arguments", "Environment flags"],
  "outputs": ["Parsed CLI commands", "Quoted shell arguments"],
  "invariants": ["SHELL_SAFE regex and positional argument parsing must correctly handle backslashes and flag ordering (verification_obligation: test CLI argument quoting)"],
  "side_effects": ["Invoking CLI sub-commands or updating state"],
  "validation_boundary": "Validates CLI option flags and command names",
  "failure_modes": ["Unknown command or invalid flag options"],
  "neighbor_needs": []
}
